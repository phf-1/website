#+OPTIONS: toc:nil author:nil

* Preliminaries

This document is best viewed using an Org Mode viewer such as Emacs.

* Introduction

See: [[https://phfrohring.com/article/5/html]]

* License

This project is licensed under the Mozilla Public License 2.0 — see: [[file:LICENSE]].

* TODO Install

TODO

* Use

#+begin_src bash :results raw :wrap example :exports both
./website help
#+end_src

#+RESULTS:
#+begin_example
usage: ./website msg

  Message              | Effect
  ---------------------+-------------------------------------------------------------
  help                 | Print this message.
  ---------------------+-------------------------------------------------------------
  context              | Build an environment with all necessary
                       | development dependencies.
  ---------------------+-------------------------------------------------------------
  html                 | Generate HTML for the content.
  ---------------------+-------------------------------------------------------------
  test                 | Execute tests.
  ---------------------+-------------------------------------------------------------
  dist                 | Build an archive.
  ---------------------+-------------------------------------------------------------
  install              | Install the website on the current system.
  ---------------------+-------------------------------------------------------------
  activate             | Activate the website service on the current system.
  ---------------------+-------------------------------------------------------------
  deploy ip user group | Send, install, and activate an archive on a
                       | remote server using ip and user.
  ---------------------+-------------------------------------------------------------
  deploy               | Like `deploy ip user group`, using default values.
#+end_example

* Specification
** Context

- [[ref:c708a576-6c6c-4eea-8473-011095d9ef10][String]]
- [[ref:8bf691e8-ad28-4d68-bfd9-d3c7f1c7b348][Nothing]]
- [[ref:1296a617-c19c-4fe8-ae2a-29c294493359][Just]]
- [[ref:3c47155b-35b8-4392-932a-53d82ef6fe40][Hash]]
- [[ref:76052dc6-4124-4143-bd78-b0a1e95e3127][Maybe(X)]]
- [[ref:a32325c0-5fe5-4c3c-9378-7504416d5099][Actor]]
- [[ref:40ff9c82-fbfc-445c-8191-426e200b6dd7][Keyword]]
- [[ref:7182dc53-6af9-4cfd-bced-8a9ea862f279][Bytevector]]

** ArticlePath
:PROPERTIES:
:TYPE: ca53237e-a27a-40d0-aeae-5d4bf4fd4e2f
:ID: c57ff4ce-4499-45d1-a9fc-3ce285b30a16
:END:

An ArticlePath is a path to a directory with the following structure:

#+begin_example
…/(public|private)/id
├── article.html
├── article.org
└── resource
    ├── …
    └── ….(jpeg|html|pdf|txt)
#+end_example

** ContentDirectory
:PROPERTIES:
:ID:       80f470d2-cb23-4e46-9ec9-58c2253d61f9
:END:

A ContentDirectory is a path to a directory with the following structure:

#+begin_example
./
├── public/
│   ├── 1/
│   ├── …/
│   └── n/
└── private/
    ├── x/
    ├── …/
    └── z/
#+end_example

where …/public/1, …/private/x, … : ArticlePath

** Resource
:PROPERTIES:
:TYPE: ca53237e-a27a-40d0-aeae-5d4bf4fd4e2f
:END:

A resource may be viewed as a Keyword × Id × Bytevector, where Id :≡ String.
resource#bv() is its bytevector, and similarly for resource#type() and resource#id().

** HtmlResource
:PROPERTIES:
:TYPE: ca53237e-a27a-40d0-aeae-5d4bf4fd4e2f
:END:

An HtmlResource is a resource such that resource#type() = :html and its bytevector
represents a UTF-8 string formatted as an HTML document.

** TextResource
:PROPERTIES:
:TYPE: ca53237e-a27a-40d0-aeae-5d4bf4fd4e2f
:END:

A TextResource is a resource such that resource#type() = :text and its bytevector
represents a UTF-8 string.

** JpegResource
:PROPERTIES:
:TYPE: ca53237e-a27a-40d0-aeae-5d4bf4fd4e2f
:END:

A JpegResource is a resource such that resource#type() = :jpeg and its bytevector
represents a JPEG image.

** PdfResource
:PROPERTIES:
:TYPE: ca53237e-a27a-40d0-aeae-5d4bf4fd4e2f
:END:

A PdfResource is a resource such that resource#type() = :pdf and its bytevector
represents a PDF file.

** Article
:PROPERTIES:
:TYPE: ca53237e-a27a-40d0-aeae-5d4bf4fd4e2f
:ID: cd651702-bd90-4ddc-962b-2579cd51d845
:END:

an article :≡ Article#mk(…) represents an article. It is built from a boolean that
tells if it is private or public, an id (a string), a title (a string), a last edit
date (Epoch), an HtmlResource, a TextResource and a set named data of resources of
these types: HtmlResource, PdfResource, TextResource, JpegResource.

article#id() is its id, article#title() its title, and so on for article#html() and
article#org() (the text resource). article#data(id) returns a resource such that
resource ∈ data and resource#id() = id if any; otherwise: nothing.

** TODO Comment

Given epoch : Epoch that represents the time the comment was saved, email : String
with length(email) ≤ 100 is the sender's email (so it can be answered), path : String
of the form =/article/id/html= where id identifies the article, content : String with
length(content) ≤ 5000, the text of the comment, then Comment#mk(epoch email path
content) : Comment.

** TODO Db
:PROPERTIES:
:TYPE: ca53237e-a27a-40d0-aeae-5d4bf4fd4e2f
:ID: ff90b350-b368-4cb3-b29e-d34e8fd341a3
:END:

A db :≡ Db#mk(articles layout db-path) where articles is a set of articles, layout a
Layout, db-path : Path to a sqlite3 database and provides db#article(id) which is an
article if article ∈ articles and article#id() = id, else nothing. In addition,
db#article("index") is an article computed from articles which represents the index
of the articles. db#article("1") is the article that serves as the landing
page. db#save(comment) saves comment to db-path.

** Static
:PROPERTIES:
:TYPE: ca53237e-a27a-40d0-aeae-5d4bf4fd4e2f
:ID: 7dcb4d1b-7881-4834-af1b-ce581461a56f
:END:

static :≡ Static#mk() is an actor such that Static#index(static dir) indexes all
files under the directory path dir. If a file — e.g. dir/master.js — has been
indexed, then Static#get(static master.js) returns the appropriate resource; else:
#f. An exception is raised in case of name conflicts.

** LayoutDirectory

A LayoutDirectory is a path to a directory with the following structure:

#+begin_example
./
└── article.html
#+end_example

where article.html is a file whose content is a Layout.

** Layout
:PROPERTIES:
:TYPE: f590edb9-5fa3-4a07-8f3d-f513950d5663
:ID: 6eec59f7-6386-4d28-a253-41c662e32339
:END:

A layout file is an HTML file with a single ~__BODY__~ string that is to be replaced
by the HTML content of an article. It completes the article content to form a
complete HTML string. layout :≡ Layout#mk(dir) where dir is a LayoutDirectory and
provides layout#embed(str) a Bytevector where str has replaced __BODY__ in the layout
string.

** Reply

A reply is a pair formed from a
[[https://www.gnu.org/software/guile/manual/guile.html#Responses][Response]] and a
Bytevector — i.e. its content. Reply#404, Reply#hello and perhaps others are
predefined replies.

** Env
:PROPERTIES:
:TYPE: ca53237e-a95a-4eea-9cc3-f2cc35eb68d9
:ID: a1603b4f-a95a-4eea-9cc3-f2cc35eb68d9
:END:

An env value is either "dev", "test" or "prod".

** Verb
:PROPERTIES:
:TYPE: ac0f68a7-24d6-458e-92b4-68f700a1cdfb
:ID: ac0f68a7-24d6-458e-92b4-68f700a1cdfb
:END:

Verb#get represents the GET verb of an HTTP request.

** Identity
:PROPERTIES:
:TYPE: a82de302-2587-483d-bf6b-bde6ff80fada
:ID: a82de302-2587-483d-bf6b-bde6ff80fada
:END:

An identity is a pair of a login and a password, both of them strings.

** TODO Website
:PROPERTIES:
:TYPE: f590edb9-5fa3-4a07-8f3d-f513950d5663
:END:

A Website written website :≡ Website#mk(env content layout login password db-path)
varies its logging behaviour and performance depending on the env value. An expected
identity is built from login and password: some content is accessible only if the
client sends the correct identity. A Db db of articles and a Layout are deduced from
the ContentDirectory content and layout (a directory path) value.

website#hello() is a reply whose content is just "hello". website#404() is
Reply#404. website#article(identity id :html) is a reply built from the
article#html(), provided article ∈ db and article#id() = id; else: Reply#404. A
challenge (Basic Auth) might be replied if necessary. website#article(identity id
:org) is similar. website#index() is like website#article(nothing "index"
:html). website#resource(identity id resource-id) replies article#data(resource-id)
if any, or Reply#404. website#static(name) replies with a static resource named name
if any; otherwise: Reply#404. Given comment : Comment, website#save(comment) saves
the comment to the database at db-path.

** TODO Conf
:PROPERTIES:
:TYPE: ca53237e-a27a-40d0-aeae-5d4bf4fd4e2f
:ID: 6ea23d0d-78c4-468c-9998-eaa555ecdb12
:END:

A configuration written conf :≡ Conf#mk() is the set of values read from the
environment. All environment variables start with the WEBSITE_ prefix. The IP address
and the PORT values are read from WEBSITE_IP and WEBSITE_PORT. CONTENT value is a
ContentDirectory. ENV is an env value. LOGIN and PASSWORD are strings. conf#ip() is
the IP value, conf#port() is the PORT value, conf#db() is the file path of the
sqlite3 database.

** Server
:PROPERTIES:
:TYPE: f590edb9-5fa3-4a07-8f3d-f513950d5663
:ID: 65e02573-e1ea-4463-95bc-46ce866426af
:END:

A server written server :≡ Server#mk(…) starts a web server and arranges so that the
requests it receives are sent to a Website instance w. Assuming a reply has been
computed, it is sent back to the request author.

** ./bin/start
:PROPERTIES:
:TYPE: ca53237e-a27a-40d0-aeae-5d4bf4fd4e2f
:ID: d7f8a08a-98fd-4086-af4e-dd4e2b1fa68a
:END:

./bin/start is a Unix executable that builds a Conf then a Server initialized with
the parameters deduced from the configuration.

** TODO Protocol

Provided the current machine has executed ~./website install && ./website activate~,
then a web server is executing, result of =./bin/start=. Given a browser pointing at
=https://domain= such that a DNS forwards requests to the current machine, then the
browser can expect the following protocol to be satisfied:

|------------------------------+-------------------+----------------------------------|
| Request                      | data              | Reply                            |
|------------------------------+-------------------+----------------------------------|
| GET  auth /hello             |                   | website#hello()                  |
| GET  auth /article/id/html   |                   | website#article(auth id :html)   |
| POST auth /article/id/html   | comment : Comment | website#save(comment)            |
| GET  auth /article/id/org    |                   | website#article(auth id :org)    |
| GET  auth /article/id/res-id |                   | website#resource(auth id res-id) |
| GET  auth /articles          |                   | website#index()                  |
| GET  auth /                  |                   | website#article(auth "1" :html)  |
| GET  auth /id                |                   | website#resource(auth "1" id)    |
| GET  _    /static/name.js    |                   | website#static(name.js)          |
| GET  _    /static/name.css   |                   | website#static(name.css)         |
| _                            |                   | website#404()                    |
|------------------------------+-------------------+----------------------------------|

GET is a Verb, auth may be an identity or nothing, website is a Website.

* Plan
** TODO Exercises are discussion subjects

Each exercise may be subject to discussions. They have a "discuss" button that
pre-fills a message ready to be updated and sent to the author. As a result, the
recipient of the message may respond. In this sense, the "exercise" answers back and
may be error-corrected or improved.

*** FAILED mailto

"message" cannot be:

#+begin_src html
<a href="mailto:contact@phfrohring.com">Discuss</a>
#+end_src

or something that uses "mailto"; it does not work: a click on the element won't
trigger anything in most cases.

*** TODO <form> + sqlite3

A click on the "discuss" button reveals a form with two fields: a field for an email
address so that an answer may be sent to the author, and a content field where the
author may write. A click on the "send" button builds a message with the following
content:

- method :≡ "POST"
- path :≡ path of the current article.
- email :≡ content of the email field, 100 characters max.
- content :≡ content of the content field, 5000 characters max.

The message is saved in a sqlite3 database where each row may be viewed as follows:

- Id :≡ Integer
- Email :≡ String of length ≤ 100 characters
- Path :≡ String of length ≤ 100 characters
- Content :≡ String of length ≤ 5000 characters
- Row :≡ Id × Email × Epoch × Path × Content

** TODO Development mode is implemented

By "development mode" we mean that after ~./website dev~, all source code changes are
reflected in the browser. For instance, if =master.js= is updated, then the browser
reloads.

** TODO Data collected and stored in the browser.

When a user reviews an exercise, they may rate their own performance. The generated
data is stored in their browser and never transmitted to the server or anyone else.

** TODO Review algorithms execute in the browser.

The exercise scheduling algorithm uses the stored data to schedule exercises and
maximize learning efficiency.

** TODO Python has been replaced by Guile
** TODO Bash has been replaced by Guile
** TODO Clean up the code

* Contributing

See: [[https://phfrohring.com/article/7/html][ExoCode]]
